using UnityEngine;

public class SuspicionStages : MonoBehaviour
{
    public static int suspectStage = 0; // total 4 stages

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
